phi_wall = 10

phi = @(x) phi_wall*exp(-10*x)+2;

a0 = 1;
a1 = 0.5;
a2 = 0.5;
C = 2;

Ac = @(x) exp(C-phi(x))*(a0/a1)^3;
Aa = @(x) exp(C+phi(x))

x = linspace(0,1);
plot(x,phi(x))
hold on
plot(x,Ac(x))